// Relative paths: nginx (prod) and the Vite dev proxy both map these onto the
// APIs, so the bundle is environment-independent and every call is same-origin.
export const EMPLOYEE_API = "/api/employee"
export const MANAGER_API = "/api/manager"
