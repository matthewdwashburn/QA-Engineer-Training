package cuc.steps;


// import org.openqa.selenium.WebDriver;


public class CrossRoleLifecycle {

    // private final TestContext context;

    // public CrossRoleLifecycle(TestContext context) {
    //     this.context = context;
    // }

    // private WebDriver driver() {
    //     return context.getDriver();
    // }

    // ---------------background Steps---------------------

    /* 
    Stays empty - Doesn't need its own steps, every step in cross-role-lifecycle.feature is one already defined 
    in manager review lifecycle and employee expense lifecycle. This is placeholder if any cross role functionality
    that hasn't been tested already needs to be added.
    */

}
