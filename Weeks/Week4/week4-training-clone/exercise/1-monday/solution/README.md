# Instructor note — Monday solutions

This folder can hold **reference** implementations. Do not share with trainees until after review.

Suggested artifacts:

- `solution_erd.mermaid` — one acceptable ERD variant.
- `solution_schema.sql` / `solution_seed.sql` — runnable PostgreSQL example.

If your cohort uses SQLite, adapt types (`SERIAL` → `INTEGER PRIMARY KEY AUTOINCREMENT`, etc.).
