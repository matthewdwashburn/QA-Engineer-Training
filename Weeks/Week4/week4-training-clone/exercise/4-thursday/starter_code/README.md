# JDBC pair starter — build & run

```text
cd starter_code
mvn -q compile exec:java
```

Requires **JDK 17+** and **Maven**. Creates `pair_store.db` in the current working directory (typically `starter_code/`).

After you implement DAO methods, extend `App.main` to demonstrate the full flow from `pair_programming_jdbc.md`.
